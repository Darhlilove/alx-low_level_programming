#include "main.h"

/**
 * main - Entry point
 *
 * Return: 0 always on success
 */

void print_alphabet(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++)
	{
		_putchar(i);
	}
	_putchar('\n');

	return void;
}
