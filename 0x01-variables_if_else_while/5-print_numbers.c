#include <stdio.h>

/**
 * main - Entry point
 *
 * num - placeholder for numbers
 *
 * Return: Always 0 on success
 */

int main(void)
{
	int num;

	num = 0;

	while (num < 10)
	{
		printf("%d", num); /* Print numbers */
		num++;
	}

	return (0);
}
