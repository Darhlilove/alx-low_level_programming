#include <stdio.h>

/**
 * main - this is the main function
 *
 * message: contains the string to be displayed
 *
 * Return: always 0
 */

int main(void)
{
char message[] = "\"Programming is like building a multilingual puzzle";

puts(message);
return (0);
}
